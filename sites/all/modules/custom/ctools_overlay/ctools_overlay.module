<?php
/**
 * @file
 * Core hooks and callbacks for CTools Overlay.module
 */



/**
 * Add overlay class to admin pages by default.
 * @todo  add admin page to edit the paths.
 */
function ctools_overlay_modal_paths() {
  $items = menu_get_router();
  var_dump(array_shift($items)); die();
//  dpm($items);
  $nonadmin_paths = 'events/*
calls/*
conflicts/*
schedules/*
master-calendar-events/*
productions/*
locations/*
taxonomy/term/*/delete
schedulearts_clone/clone
schedulearts_clone/clone/*
node/*/repeats
node/*/date_repeat_instance_edit
node/*/date_repeat_instance_edit/*
schedulears_print/select-timeframe';

  foreach ($items as $path => &$info) {
    if (path_is_admin($path) || drupal_match_path($path, $patterns)) {
      $info[$path] = array();
    }
  }
  dpm(array_keys($info));
  return $info;
}
